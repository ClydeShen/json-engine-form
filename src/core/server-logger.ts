/**
 * Logs a message with the library prefix (server-side)
 * @param message - The message to log
 */
export function logMessageServer(message: string): void {
  console.log(`[JSON-Engine-Form Server]: ${message}`);
}
