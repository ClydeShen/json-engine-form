'use client';

/**
 * Logs a message with the library prefix (client-side)
 * @param message - The message to log
 */
export function logMessageClient(message: string): void {
  console.log(`[JSON-Engine-Form Client]: ${message}`);
}
